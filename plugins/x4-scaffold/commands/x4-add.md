---
description: Add a new mobile app or web app to an existing x4-mono project.
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion
---

Run the `/x4-add` skill to add a new app to your x4-mono project.

Must be run from inside an x4-mono project root. Supports adding
mobile apps (Expo 52) and web apps (Next.js 15).
