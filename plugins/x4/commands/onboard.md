---
description: Check your local dev environment and walk through setup for tools, accounts, and CLI access needed for x4-mono development.
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion, WebFetch
---

Run the `/onboard` skill to check and set up your development environment.

Detects installed tools (Bun, Node, Git, gh), checks account access
(Neon, Railway, Vercel, Anthropic), installs missing Claude Code plugins,
and walks through setup for anything that's missing. Safe to re-run anytime.
