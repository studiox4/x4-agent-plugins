---
description: Diagnose project setup — checks prerequisites, config, database, agents, env vars, and companion plugins
allowed-tools: [Read, Glob, Grep, Bash]
---

Run the `/doctor` skill to check your project's x4 setup and find issues
before they cause problems during `/x4:work`.