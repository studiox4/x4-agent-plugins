---
description: Set up or update environment variables for an x4-mono project — database, auth secrets, AI keys.
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion
---

Run the `/env` skill to configure your x4-mono project's environment.

Interactive wizard for database connection (Neon auto-create or paste URL),
authentication secrets (auto-generate or paste), and AI API keys.
Writes to `.env.local`.
